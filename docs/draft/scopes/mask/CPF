#This is extHTML, so it could have specific browser language
Mask cpf:
    JS:
        get:
            return this.value
        set(v):
            this.value = 
                v
                .Validation:cpf
    HTML:
        get:
            this.value
        set:
            $.mask(this)
    
    DB:
        Ordered????
        History????





Mask cpf:
    CODE:
        string get():
        string set(v):
    VIEW:
        get:
        set:

    PERSISTENCE:


val
mask=0000 0.000
    Converter o estado em memória na forma como o usuário vai interagir

    -1 => red
    0 => amarelo
    1 => verde


Memory
    Database format
    Nenhum dado tem mascara
        - É uma função de visualização

